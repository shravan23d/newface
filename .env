# Environment variables for Emergency Face Tracker
# Copy this file and adjust values as needed.
# For local secrets, use .env.local (not committed).

NODE_ENV=production
JWT_SECRET=change-this-to-a-long-random-string-in-production
ADMIN_EMAIL=admin@emergencyface.local
ADMIN_PASSWORD=admin123
